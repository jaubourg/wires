#!/usr/bin/env node

"use strict";

require( "resolve" )( "wires/bin/cli", {
    "basedir": process.cwd(),
}, function( _, filename ) {
    /* eslint-disable no-console */
    require( filename || "./cli" )( {
        "argv": process.argv,
        "stdio": "inherit",
        "log": console.log,
        "error": console.error,
        "exit": function( code ) {
            // eslint-disable-next-line no-process-exit
            process.exit( code );
        },
    } );
} );
